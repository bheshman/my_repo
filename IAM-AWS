CREATE A ROOT USER ACCOUNT
[THEN IN SERVICES MOVE TO IAM SERVICE
CREATE A USER THEN CLICK ON Provide user access to the AWS Management Console 
THEN SELECT I I want to create an IAM user
THEN NEXT STEP CONSOLE PASSWORD SELECT AUTOGENERATE PASSWORD
THEN IAM USER ACCOUNT IS CREATED IN ROOT USER ACC] - THIS AUTHENTICATION BY ROOT USER
GIVING PERMISSIONS
